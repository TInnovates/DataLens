apiVersion: v1
kind: Service
metadata:
  name: datalens-backend-service
spec:
  type: LoadBalancer
  selector:
    app: datalens-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500

